// The app is a client-rendered SPA: it boots its store and loads data in the
// browser (see store.bootstrap()). Access is already gated server-side by
// hooks.server.js, so there's nothing to server-render here.
export const ssr = false;
